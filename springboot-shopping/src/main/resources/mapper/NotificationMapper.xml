<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaoshan.springbootdemo.mapper.NotificationMapper">

    <insert id="insert">
        INSERT INTO notifications (id, user_id, type, title, content, extra_data, is_read, created_at)
        VALUES (#{id}, #{userId}, #{type}, #{title}, #{content}, #{extraData}, 0, NOW())
    </insert>

    <select id="findByUserId" resultType="com.xiaoshan.springbootdemo.entity.Notification">
        SELECT * FROM notifications
        WHERE user_id = #{userId}
        <if test="type != null and type != 'ALL'">AND type = #{type}</if>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM notifications WHERE user_id = #{userId}
        <if test="type != null and type != 'ALL'">AND type = #{type}</if>
    </select>

    <select id="countUnread" resultType="int">
        SELECT COUNT(*) FROM notifications WHERE user_id = #{userId} AND is_read = 0
    </select>

    <update id="markAsRead">
        UPDATE notifications SET is_read = 1 WHERE id = #{id}
    </update>

    <update id="markAllAsRead">
        UPDATE notifications SET is_read = 1 WHERE user_id = #{userId} AND is_read = 0
    </update>

</mapper>