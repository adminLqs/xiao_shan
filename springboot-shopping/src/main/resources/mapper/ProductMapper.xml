<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xiaoshan.springbootdemo.mapper.ProductMapper">

    <!-- ProductVO 结果映射 -->
    <resultMap id="ProductVOMap" type="com.xiaoshan.springbootdemo.entity.vo.ProductVO">
        <!-- 基础字段 -->
        <id property="id" column="id"/>
        <result property="sellerId" column="seller_id"/>
        <result property="categoryId" column="category_id"/>
        <result property="name" column="name"/>
        <result property="brand" column="brand"/>
        <result property="description" column="description"/>
        <result property="detailHtml" column="detail_html"/>
        <result property="weight" column="weight"/>
        <result property="isFreeShipping" column="is_free_shipping"/>
        <result property="serviceGuarantee" column="service_guarantee"/>
        <result property="deliveryCity" column="delivery_city"/>
        <result property="viewCount" column="view_count"/>
        <result property="salesCount" column="sales_count"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        
        <!-- SKU 汇总字段 -->
        <result property="price" column="price"/>
        <result property="originalPrice" column="original_price"/>
        <result property="stock" column="stock"/>
        
        <!-- 关联查询字段 -->
        <result property="categoryName" column="category_name"/>
        <result property="commentCount" column="comment_count"/>
    </resultMap>

    <!-- 查询商品详情（返回 ProductVO） -->
    <select id="findProductVOById" resultMap="ProductVOMap">
        SELECT p.id, p.name, p.brand, p.description, p.detail_html,
               p.sales_count, p.view_count,
               p.category_id, p.seller_id, p.status, p.created_at, p.updated_at,
               p.weight, p.is_free_shipping, p.service_guarantee, p.delivery_city,
               (SELECT SUM(stock) FROM product_skus WHERE product_id = p.id) as stock,
               (SELECT MIN(price) FROM product_skus WHERE product_id = p.id) as price,
               (SELECT MAX(original_price) FROM product_skus WHERE product_id = p.id AND original_price IS NOT NULL) as original_price,
               c.name as category_name,
               (SELECT COUNT(*) FROM reviews r WHERE r.product_id = p.id) as comment_count
        FROM products p
        LEFT JOIN categories c ON p.category_id = c.id
        WHERE p.id = #{productId}
    </select>

</mapper>